// const { User } = require('../models');

// const create = async (display_name, email, password, image ) => {
//   const createUser = await User.create();
//   return createUser;
// };

// module.exports = create;